module.exports = {
  roots: ["<rootDir>/src"], // Directorio raíz para las pruebas
  testMatch: ["**/?(*.)+(spec|test).[jt]s?(x)"], // Patrón de nombres de archivos de prueba
  moduleFileExtensions: ["js", "ts", "tsx", "json", "node"],
};
